# 🔧 Обновяване на CNAME за www

## Текущо състояние:
- ✅ A запис за `marbaras.com.` сочи към `66.33.22.165` (Railway IP) - ПРАВИЛНО
- ❌ CNAME за `www` сочи към `1v861vtj.up.railway.app` (СТАРА стойност)
- ✅ Railway очаква: `ej8fghaw.up.railway.app`

## Стъпка: Обнови CNAME за www

1. В jump.bg DNS мениджъра, намери CNAME запис за `www`
2. Кликни на редактиране (или изтрий и добави отново)
3. Редактирай стойността:
   - Хост: `www` (без точка в края)
   - Тип: `CNAME`
   - Стойност: `ej8fghaw.up.railway.app` (БЕЗ точка в края)
   - TTL: `14400` или `3600`
4. **ВАЖНО:** Кликни **"Запази промените"** (син бутон)

## След запазване:

1. Изчакай 5-10 минути
2. Отиди на Railway → Settings → Domains
3. Намери `www.marbaras.com`
4. Кликни **"Check DNS"** бутона
5. Railway трябва да покаже "Active" (зелена отметка)

## Проверка:

След промяната, използвай:

```bash
# Проверка на CNAME (трябва да видиш новата Railway стойност)
dig www.marbaras.com CNAME +short
```

**Очакван резултат:** `ej8fghaw.up.railway.app.`

---

## Важно:

- Убеди се че CNAME стойността е точно `ej8fghaw.up.railway.app` (БЕЗ точка в края)
- След редактиране, задължително кликни "Запази промените"
- Изчакай 15-30 минути за DNS да се разпространи

